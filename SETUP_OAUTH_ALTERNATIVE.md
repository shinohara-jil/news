# 組織ポリシー適用時の代替手順（OAuth 2.0詳細）

組織ポリシーによりサービスアカウントキーが作成できない場合の、詳細な手順です。

## 前提条件

- Google Cloud プロジェクトが作成済み
- Google Sheets APIが有効化済み

## 手順

### 1. OAuth同意画面の設定（初回のみ必要）

1. [Google Cloud Console](https://console.cloud.google.com/)にアクセス
2. 左側メニューから「APIとサービス」→「OAuth同意画面」を選択
3. ユーザータイプを選択：
   - **外部**（個人のGoogleアカウントを使用する場合）を選択
   - 「作成」をクリック
4. アプリ情報を入力：
   - **アプリ名**: すでに「News Site」が入力されているので、そのままでOK（変更も可）
   - **ユーザーサポートメール**（⚠️ 必須）:
     - 「ユーザーサポートメール *」のドロップダウンをクリック
     - 自分のGoogleアカウントのメールアドレスを選択
     - もしメールアドレスが表示されない場合：
       * ドロップダウン内の入力欄に直接メールアドレスを入力することもできます
       * または、プロジェクトの所有者メールアドレスが自動的に候補として表示されます
   - **デベロッパーの連絡先情報**:
     - 「デベロッパーの連絡先情報」フィールドに自分のメールアドレスを入力
     - これは後で設定できる場合もあります
   - 赤いエラーメッセージ「メールアドレスを選択する必要があります」が消えるまで、メールアドレスを選択/入力してください
   - 「次へ」ボタンをクリック（メールアドレスを選択すると「次へ」がクリック可能になります）
5. スコープの設定：
   - 「スコープを追加または削除」をクリック
   - 以下のスコープを追加：
     - `https://www.googleapis.com/auth/spreadsheets`（スプレッドシートの読み書き）
   - 「更新」→「保存して次へ」をクリック
6. テストユーザーの追加：
   - 「ユーザーを追加」をクリック
   - 自分のGoogleアカウントのメールアドレスを追加
   - 「保存して次へ」をクリック
7. 概要を確認して「ダッシュボードに戻る」をクリック

### 2. OAuth 2.0クライアントIDの作成

1. 左側メニューから「APIとサービス」→「認証情報」を選択
2. 画面上部の「認証情報を作成」をクリック
3. **「OAuth クライアント ID」**を選択
4. アプリケーションの種類で「ウェブアプリケーション」を選択
5. 名前を入力（例：「News Site」）
6. **承認済みのリダイレクト URI**：
   - 開発環境: `http://localhost:3000/api/auth/callback`
   - 本番環境: `https://your-domain.vercel.app/api/auth/callback`（後で追加可能）
   - 「+ URIを追加」で追加
7. 「作成」をクリック
8. **重要：** 表示されるダイアログで以下をコピーして保存：
   - **クライアント ID**（例：`123456789-abc.apps.googleusercontent.com`）
   - **クライアント シークレット**（例：`GOCSPX-xxxxxxxxxxxxx`）
   - ⚠️ これらは一度しか表示されません！必ず保存してください

### 3. 環境変数の設定

プロジェクトルートに `.env.local` ファイルを作成：

```env
# OAuth 2.0認証情報
GOOGLE_CLIENT_ID=あなたのクライアントID
GOOGLE_CLIENT_SECRET=あなたのクライアントシークレット
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/callback

# スプレッドシートID
SPREADSHEET_ID=あなたのスプレッドシートID

# リフレッシュトークン（初回認証後に自動生成されます）
GOOGLE_REFRESH_TOKEN=
```

### 4. 注意事項

- **開発環境**：初回実行時にブラウザでGoogleアカウントへのログインが求められます
- **本番環境（Vercel）**：サーバーサイドで実行するため、初回認証フローを実装する必要があります
- **トークンの有効期限**：リフレッシュトークンは期限が長いですが、失効する場合があります
- **スコープ**：スプレッドシートの読み書きのみが許可されます

### 5. 実装時の考慮事項

OAuth 2.0を使用する場合、コード側で以下を実装する必要があります：

1. **初回認証フロー**：
   - ユーザーをGoogleの認証画面にリダイレクト
   - 認証後、アクセストークンとリフレッシュトークンを取得
   - リフレッシュトークンを環境変数またはデータベースに保存

2. **トークンの更新**：
   - アクセストークンは1時間で期限切れ
   - リフレッシュトークンを使用して新しいアクセストークンを取得

3. **Vercelでの実行**：
   - API Routeで認証フローを処理
   - リフレッシュトークンは環境変数として設定（手動または自動）

実装コードは次のステップで提供します。
